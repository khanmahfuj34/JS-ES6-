const poem = `Roses are red
Violets are blue
Sugar is sweet
And so are you.`;
console.log(poem);

function sum(num1, num2) {
    const result = num1 + num2;
    const output = `the sum of ${num1} and ${num2} is ${num1 + num2}`;
    console.log(output);
}
sum(10, 10);
const price = 100;
const discountPrice = `The price of ${price} after discount ${price*0.8}`;
console.log(discountPrice);